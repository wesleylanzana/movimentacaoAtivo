//continuar selecionando primeiro todos os campos pai e filho do Valor Unitário, utilizar um each ou forEach.



/*$(document).ready(function() {
    // Inicializa a máscara financeira no campo "valor" com o símbolo R$

    var campoFilhoValorUnitario = $("input[id^='valorUnitario___']").val(); //responsável por acessar todos os campos de Valor Unitário da tabela pai e filho

    console.log(campoFilhoValorUnitario);

    $(campoFilhoValorUnitario).maskMoney({
        prefix: 'R$ ',
        allowNegative: false,
        thousands: '.',
        decimal: ',',
        affixesStay: true
    });
});*/